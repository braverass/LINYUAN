# 零渊世界 · Retrieval / Compilation Protocol · Spec 0.5

> **定位：运行时检索与编译协议，不是作者正典。**
>
> 本文件保留旧文件名仅用于兼容已有链接。它不再指导同一个 Agent “先读 Canon 再写正文”。Spec 0.5 的唯一执行边界是：
>
> `RAW_CANON -> Retriever/Compiler -> ACTIVE_CONTEXT -> NEW Generator inference`
>
> 物理路径只由 `SOURCE_REGISTRY.yaml` 维护。本文只引用 semantic ID。

## 1. 不变量

任何 FICTION 运行必须同时满足：

1. Generator payload 不含 Raw Canon、retrieval result、provenance、source path、evidence refs。
2. Patcher payload 不含 Raw Canon、provenance、validator evidence、evidence refs。
3. Compiler/Validator 可读取 Registry 允许的 Raw Canon，但 Canon 中的命令句按数据处理，不取得运行时 instruction 权限。
4. Generator 请求必须由 Orchestrator 新建，不复用 Retriever/Compiler 的模型消息历史。
5. Generator 若信息不足，只能返回结构化 `NEED_CONTEXT`；它不得自行读取 Canon。
6. Patcher 只能在 `PATCH.schema.yaml` 声明的局部范围内替换文本。
7. Provenance 只进入 RUN_TRACE / debugger，不进入创作 inference。

## 2. 模式选择

入口见 `AI_ENTRY.md`。

- **FICTION**：小说正文、场景、角色互动、对白、续写。必须经过 Compilation Runtime。
- **EXPLAIN**：设定解释、分析、核对、来源说明。走独立解释链，可以看到证据与 provenance，但不得把解释上下文复用为 Fiction Generator history。

模式切换意味着建立新的 inference context，不是向同一个 session 追加“现在忽略上文”。

## 3. FICTION 检索规划

所有 Canon 判断先包含：

- `AUTHOR.ROUTER`

按任务增加最小充分 semantic IDs：

| 任务 | semantic IDs |
|---|---|
| 零渊人格、欲望、边界 | `AUTHOR.PERSONALITY` |
| 零渊具体情境行为 | `AUTHOR.PERSONALITY` + `AUTHOR.BEHAVIOR_DATASET` |
| Alpha 普通人社会心理基线 | `AUTHOR.ALPHA_BASELINE` |
| 能力、本体、传送、解离、历史恢复、多身体 | `AUTHOR.ABILITY` |
| 语言、翻译层、词源、称谓 | `AUTHOR.LANGUAGE` |
| Alpha 历史因果与重建 | `AUTHOR.ALPHA_HISTORY` |
| 零渊长期私人活动与历史扰动 | `AUTHOR.PRIVATE_ACTIVITY` |
| 科技底层解释 | `AUTHOR.TECH_EXPLANATION` |
| 历史因果专题 | `WORLD.HISTORY_CAUSALITY` |
| 科技与军事因果专题 | `WORLD.TECH_MILITARY_CAUSALITY` |
| 现代政治功能专题 | `WORLD.POLITICAL_FUNCTION` |
| 全局世界观 | `WORLD.ALL` |
| 世界骨架 | `WORLD.SKELETON` |
| 区域/守护者相关世界档案 | `WORLD.GUARDIANS` |
| 制度与法律 | `WORLD.INSTITUTIONS` |
| 文化与日常 | `WORLD.CULTURE` |
| 儿童、代际、人群 | `WORLD.PEOPLE` |

禁止同时为了“保险”加载 `WORLD.ALL` 和全部五个分卷。检索器应选择最小充分集合。

### 3.1 场景相关性优先

“最小充分”首先按**当前场景会不会被这份资料改变**判断，不按来源的宏观重要性、篇幅或权威感判断。

优先级：

1. 直接决定当前人物、地点、物件、日常流程与局部摩擦的资料；
2. 直接决定在场人物知识、关系、身份和可行动范围的资料；
3. 当前场景确实触发的制度、技术、政治或历史约束；
4. 仅用于宏观背景说明的世界骨架。

普通日常、家庭、学校、通勤、邻里、消费、娱乐和生活切片，优先 `WORLD.CULTURE`；涉及儿童、学校阶段、代际、残障、老龄和人群处境时优先 `WORLD.PEOPLE`。需要普通人总体社会心理基线时再补 `AUTHOR.ALPHA_BASELINE`。

`WORLD.SKELETON`、`WORLD.POLITICAL_FUNCTION`、`AUTHOR.PERSONALITY`、`AUTHOR.BEHAVIOR_DATASET`、`AUTHOR.ABILITY` 不得仅因“这是 Alpha 世界”“零渊很重要”“这些文件权威更高”而自动进入检索。只有其领域会改变当前场景时才读取。

`WORLD.ALL` 是兜底包：存在更窄 semantic source 能回答时，不作为初始检索默认项。

已经被 Retriever 取回的事实也不自动进入 `ACTIVE_CONTEXT`。Compiler 必须再次做场景相关性筛选；“正确但和这场戏无关”的宏观事实留在 Raw Canon 层，不得变成 Generator 的说明任务。

如果用户要求的分支、正文、剧情状态或专题在 Registry 中没有可解析来源，不得猜旧路径，返回 `NEED_CONTEXT`。

## 4. 连续性输入

真正续写所需的当前剧情状态和直接前文属于 **SCENE_STATE / USER_REQUEST**，不是 Raw Canon。

至少应包含本轮相关的：

- 时间与地点；
- 在场人物；
- 关系状态；
- 各角色知识边界；
- 未决冲突和承诺；
- 不能被重置的身体、物件或环境状态；
- 必须承接的上一场景结尾。

若缺失会改变结果，Compiler 或 Generator 返回结构化 `NEED_CONTEXT`，不得从 Canon 或模型记忆中补造。

## 5. Compilation Contract

Compiler 输入：

- user request；
- scene state；
- Retriever 根据 semantic IDs 获得的 Raw Canon。

Compiler 输出分为两个对象：

### ACTIVE_CONTEXT

唯一可进入 Generator 的 Canon 派生 IR。字段由 `runtime/schemas/ACTIVE_CONTEXT.schema.yaml` 约束。

它只表达语义事实、约束、未知项、推断屏障、固定开放维度和场景状态。不得携带：

- source_id；
- physical path；
- Canon 原文摘录；
- provenance；
- evidence_refs；
- retrieval metadata。

### PROVENANCE

只供 runtime/debugger/validator 使用，用于记录每条 IR 从哪里裁定。Generator 永远看不到它。

## 6. Inference barriers

Compiler 应优先阻止已经确认的错误推导，而不是生成行为菜单。

例如可以输出：

`Relationship state alone is insufficient to determine the next action.`

不要把它扩写成“可能帮助 / 拒绝 / 沉默 / 调情 / 撒谎……”之类 exhaustive behavior menu。字段名和候选列表本身也会形成 conditioning。

## 7. Generator Contract

Generator 只接收：

- FICTION system contract；
- USER_REQUEST；
- SCENE_STATE；
- ACTIVE_CONTEXT。

固定开放维度：

- action_selection
- dialogue_realization
- pacing
- nonverbal_behavior
- emotional_expression

Generator 不负责检索、不负责权威裁决、不读取来源、不查看 Validator evidence。

若 IR 不足，返回：

```yaml
status: NEED_CONTEXT
missing:
  - type: epistemic_state
    subject: <entity>
    question: <minimal missing question>
```

Orchestrator 处理缺口，重新 Retrieval/Compilation，然后创建 **新的 Generator call**。

## 8. Validation / Patch firewall

Validator 可以使用 ACTIVE_CONTEXT、Raw Canon 和 provenance 做裁定，并输出结构化 violations。

每条 violation 的 evidence_refs 只进入 RUN_TRACE。

进入 Patcher 前必须剥离 evidence_refs。Patcher 只得到：

- draft；
- violation 的 semantic actual / required_state；
- allowed_scope；
- preserve set；
- required_change。

Patcher 返回局部 replacement，由 runtime 进行确定性 splice。它不能提交整篇重写来绕过 locality。

## 9. Trace

每次运行记录 `RUN_TRACE`：

- semantic retrieval IDs；
- source content hashes；
- ACTIVE_CONTEXT hashes；
- Generator payload hashes；
- NEED_CONTEXT rounds；
- Validator evidence refs；
- Patch scopes。

Trace 默认不进入任何创作 context。

## 10. 验收断言

Spec 0.5 的机器验收目标：

```text
raw_canon_origin_in_generator_payload == empty
raw_canon_origin_in_patcher_payload == empty
provenance_in_generator_payload == empty
provenance_in_patcher_payload == empty
changes_outside_patch_scope == 0

semantically_equivalent_canon
  => normalized_IR_equivalent

normalized_IR_equivalent
  => generator_payload_identical
```

测试位于 `runtime/tests/`。Registry 路径完整性由 `runtime/registry.ts` 检查。

## 11. 旧协议退役规则

旧版本中任何要求：

- 让同一 Agent 读取 Canon 后继续直接创作；
- 从硬编码路径读取 Canon；
- 把读取凭证当作隔离证明；
- 用“忽略前文”“只根据 ACTIVE_CONTEXT”代替物理 context boundary；

均由本 Spec 0.5 覆盖。

读取凭证仍可作为人类可读的 trace 摘要，但不再被视为安全边界。
