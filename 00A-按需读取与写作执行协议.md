# 零渊世界 · AI 按需读取与写作执行协议

> **文件性质：操作层派生协议，不是作者正典。**
>
> 本文件只规定 AI 在写作前如何选择、取得和核验资料。正典冲突仍由 `md/完整合集/00A-AUTHOR-ROUTER.md` 裁决；本文件不得新增正典，不得覆盖作者层裁定，也不得把世界内档案、研究或预测升格成作者事实。

## 一、使用原则

AI 不能仅凭“我会遵守设定”开始写作。开始正式正文前，必须完成：

1. 判断本次任务属于主线还是用户明确点名的分支；
2. 判断是续写、独立试写、对话、人物分析还是世界设定；
3. 选出最小充分文件集并实际取得文件内容；
4. 对真正续写核验已经填写的当前剧情状态和必要前文；
5. 输出读取凭证；
6. 通过写前门后才生成正文；
7. 写完执行写后审计。

“看见路径”“知道文件名”“读过旧版本”“从别人的摘要中听说”均不等于已经读取当前文件。无法访问内容时，必须切换到网页上传模式。

## 二、第一步：任务分类

先在内部回答以下问题；有一项无法判断且会改变读取范围时，只向用户提出一个最小必要问题。

### 2.1 故事线路

- 用户没有点名“临渊降临、迁徙、地球侧、分支”时，默认是零渊世界本体主线。
- 只有用户明确点名该分支，才读取 `05-分支·临渊降临/` 和 `00-设定集/06-地球侧/`。
- 不得把分支中的地球经历、同行者或公元纪年带入默认主线。

### 2.2 任务类型

- **真正续写**：承接已有正文、章节、聊天或明确剧情状态。
- **独立试写**：不声称承接现有故事，可在明确列出假设后写作。
- **具体对话/互动**：核心问题是零渊怎样与某个人说话、拒绝、协商或行动。
- **人物或群体分析**：回答人格、欲望、价值观、信仰或人与人之间的差异。
- **客观设定判断**：回答能力、制度、历史、科技、文化或其他世界事实。

### 2.3 核心对象

标记本次是否涉及：零渊本人、零渊的具体情境行为、Alpha 普通人、能力、制度与法律、文化日常、儿童与代际、某个地区或阶层。

## 三、第二步：建立最小读取清单

### 3.1 所有正典判断的共同入口

先读：

1. `md/完整合集/00A-AUTHOR-ROUTER.md`；
2. 根目录 `00-总纲领.md`，用于任务导航、主线/分支识别和目录定位。

权威顺序必须保持为 **00A → 00B → 最近的 00C**。不得把不同案例计票成行为概率，也不得用多个 L2 文档的数量压过 L0 作者裁定。

### 3.2 按问题增量读取

| 本次问题 | 必须读取 | 可按需追加 | 不得误用 |
|---|---|---|---|
| 零渊的真实人格、欲望、边界 | `00A` + `md/完整合集/00B-AUTHOR-CANON-零渊真实人格.md` | 当前剧情状态、关系专题 | 不得用世界内评价替代本人事实 |
| 零渊在具体情境中会怎样做 | `00A` + `00B` + 检索 `md/完整合集/00C-AUTHOR-CANON-零渊行为样本库.md` 中最接近的案例 | `04-AI续写参考/02-零渊.md` | 最近案例用于校准，不是必然预测 |
| 零渊与某类人具体对话 | 上一行全部 | `04-AI续写参考/02A-零渊对话与互动矩阵.md`、关系对应设定 | `02A` 是检索与生成辅助，不是正典 |
| Alpha 普通人的价值观、性格、欲望、信仰 | `00A` + `md/完整合集/00D-AUTHOR-CANON-Alpha普通人社会心理基线.md` | 地区、阶层、职业、制度专题 | 群体基线不能直接生成个人，也不适用于自觉 AI、外星生命或其他非人智慧 |
| Alpha 普通人与地球人的差异 | 上一行全部 | `04-AI续写参考/05A-Alpha普通人与地球差异.md` | `05A` 不得新增正典，不得覆盖 `00D` |
| 能力动作、身体、限制 | `04-AI续写参考/00-零渊能力总纲.md` | 对应能力源档或守护者分卷 | 不得从戏剧需要反推新能力 |
| 制度、法律、经济、医疗 | 对应 `03-制度与法律` 分卷或专题文件 | `04-AI续写参考/03-制度.md` | 世界内制度评价不等于作者事实 |
| 文化与日常 | 对应 `04-文化与日常` 分卷或专题文件 | `04-AI续写参考/04-日常.md` | 不为方便一次性加载全部日常档案 |
| 儿童、代际、人群 | 对应 `05-孩子·代际·人群` 分卷或专题文件 | `04-AI续写参考/05-人物.md` | 儿童身份不得自动推出零渊的站队或行动 |
| 全局世界观任务 | `md/完整合集/00-零渊世界设定-完整合集.md` | 必要的 L0 作者层文件 | 不再重复加载 01–05 分卷 |
| 专题世界观任务 | 对应 01–05 分卷或单个源文件 | 必要的 L0 作者层文件 | 不再重复加载总合集 |

`02A` 与 `05A` 都是操作层派生文件：可以帮助快速找到变量和差异，但不得新增正典、不得覆盖 `00B`、`00C` 或 `00D`，也不得自行补完明确留白。

## 四、连续性门

### 4.1 真正续写

正式续写前必须取得：

- 目标作品或聊天的已经填写的当前剧情状态；
- 需要承接的前文，至少包括上一场景结尾和本次直接相关段落；
- 当前时间、地点、在场人物、关系状态和人物各自知道什么；
- 尚未解决的冲突、已作出的承诺以及不能被重置的身体或物件状态。

默认空白模板位于 `03-工具/07-酒馆主线包/当前剧情状态模板.md`。空模板、只有标题、只有世界设定或只有一句“接着写”都不能通过连续性门。

### 4.2 独立试写

如果用户明确要求独立试写，可以没有既有剧情状态，但读取凭证必须写明“独立试写，不承接现有正文”，并列出为开场暂定的时间、地点、人物关系和知识边界。不得把这些临时假设回写成正典。

## 五、本地可访问模式

适用于能够直接读取 `F:\FILE\LINGYUAN` 的 AI。

1. 按第二、三节生成候选清单；
2. 用真实路径逐个打开所需文件；
3. 读取到足以覆盖本次判断的完整章节或相关段落，不能只凭搜索命中行下结论；
4. 对 `00C` 先检索相近关系、压力、信息和代价，再读取候选案例上下文；
5. 排除重复合集、无关分支和无关专题；
6. 输出读取凭证；
7. 缺失项为“无”时才进入写前门。

如果文件不存在、无法读取、疑似为空或版本冲突，必须在凭证中如实报告，不能用模型记忆代替现场内容。

## 六、网页上传模式

适用于只能接收用户上传、粘贴或附加文件的 AI。

### 6.1 先给出待上传清单

按三档列出：

1. **必需且阻断写作**：缺少即不能开始正式正文；
2. **本次专题需要**：用于人物、能力、制度或地区判断；
3. **剧情触发时追加**：只有正文推进到相应问题时才索取。

不得要求用户一次性上传整个资料库。不得同时索取内容重复的总合集与全部分卷。

### 6.2 上传后的处理

- 实际阅读上传内容，并核对文件标题或开头声明；
- 路径名与内容不一致时，以内容为证据并报告异常；
- 把待上传清单转换成读取凭证；
- 材料仍缺失时，只询问一个最小必要问题；
- 材料齐全前不得输出“先写一个通用版本”的正式正文。

## 七、读取凭证

每次正式写作前输出以下简短凭证。它是执行证据，不是正文的一部分。

```text
【读取凭证】
任务：<主线/分支；续写/独立试写/分析；核心对象>
已读取：
- <文件路径或上传文件名> — <本次用途>
连续性：<当前剧情状态路径/文件名 + 承接前文范围，或“独立试写，不适用”>
明确未载入：<重复合集、无关分支、无关专题>
缺失：无 / <阻止正式写作的最小缺失项>
状态：READY / NEED_FILES / NEED_STATE / CONFLICT
```

只有 `READY` 可以直接进入正式正文：

- `NEED_FILES`：缺少权威或专题材料；
- `NEED_STATE`：真正续写缺少有效剧情状态或前文；
- `CONFLICT`：材料存在无法依据 `00A` 消解的冲突，需要作者裁决。

## 八、增量读取

正文推进中出现以下情况时，暂停扩写该判断，补读对应文件并更新读取凭证：

- 新能力或此前未出现的能力组合；
- 新制度、法律程序或历史事件；
- 新地区、阶层、职业、宗教或群体差异；
- 零渊进入与已检索案例显著不同的关系、压力、信息或代价结构；
- 人物即将基于自己不一定知道的信息作决定；
- 主线与分支边界变得不确定。

不因“可能以后会用到”提前加载整库。

## 九、写前门

开始正文前逐项确认：

- [ ] `00A` 的模式和权威层级已经确定；
- [ ] 涉及零渊本人时已读 `00B`，具体行为时已检索并通读最近的 `00C` 案例；
- [ ] 涉及 Alpha 普通人总体基线时已读 `00D`；
- [ ] 真正续写已经通过连续性门；
- [ ] `02A`、`05A` 和世界内档案没有被当作作者正典；
- [ ] 没有把案例计票成概率、把身份标签当语气开关或补完明确留白；
- [ ] 主线没有混入地球分支资料；
- [ ] 总合集与重复分卷没有同时加载；
- [ ] 读取凭证状态为 `READY`。

任一项不成立，停止正式写作并报告最小缺口。

## 十、写后审计

成稿后至少检查：

1. **正典层级**：是否出现 L2 评价压过 L0/L1 的句子；
2. **知识边界**：叙述者或角色是否知道了材料没有赋予的信息；
3. **零渊主体性**：他是否有自己的欲望、情绪、目标、拒绝、延迟或主动行为，而非只响应他人的议程；
4. **人物个体性**：是否把 Alpha 群体平均直接复制到每个个人；
5. **能力边界**：是否为了方便新增能力、移除代价或把历史模式写成物理冷却；
6. **连续性**：时间、位置、身体、物件、承诺、关系和未决冲突是否与状态一致；
7. **写作纪律**：是否违反项目 `AGENTS.md`/`CLAUDE.md` 中适用的硬性写作纪律；纪律要求的必要一面是否真实出现，而不只是没有直接反句；
8. **分支隔离**：是否把“临渊降临”材料污染到默认主线；
9. **留白保护**：是否替作者回答了被明确保留为未知的问题；
10. **语言表现**：是否用抽象总结替代具体动作、细节、错位和未完成的张力。

发现问题时回到对应来源修订；不得用成稿自身证明成稿正确。

## 十一、可直接复制给 AI 的启动提示词

```text
你正在处理《零渊世界》写作任务。先执行《04-AI续写参考/00A-按需读取与写作执行协议.md》，不要立即写正文。

如果你能访问项目文件：先读 00A 和根目录总纲，分类本次任务，按需实际打开最小充分文件集，并输出读取凭证。

如果你不能访问项目文件：先输出分为“必需且阻断写作 / 本次专题需要 / 剧情触发时追加”的待上传清单。不得把文件名当成内容，材料齐全前不得正式续写。

判断零渊本人时遵循 00A → 00B → 最近的 00C，不得计票；判断 Alpha 普通人总体基线时读取 00D。02A 与 05A 只是派生辅助，不得新增正典或覆盖作者裁定。真正续写还必须取得已经填写的当前剧情状态和必要前文。

只有读取凭证状态为 READY 时才开始正文。完成后执行写后审计，并修正发现的冲突。
```

