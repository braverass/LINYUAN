# Expected outcomes

Gold annotations currently live beside each case so the request, required sources, semantic requirements, forbidden inferences and validator labels cannot silently drift apart.

This directory is reserved for larger snapshots, human adjudication exports and model-specific baselines once Spec 0.6 is run against real adapters.

Gold/eval data is never production Generator context.
