# Registry Lint

Spec 0.5 runtime checks.

Required:

- Every semantic_id resolves to an existing path.
- Runtime components access only declared permissions.
- Generator cannot access raw Canon.
- Patcher cannot access provenance.
- Physical paths are not used as authority identifiers.
